<h1><%= @post.title %></h1>

<p>Body: <%= @post.body %></p>

<%= link_to 'Delete Post',
    post_path(@post),
    method: :delete,
    data: { confirm: 'Are you sure that you want to delete this post? '}
%>